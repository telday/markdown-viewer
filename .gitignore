.build/
.swiftpm/
*.xcodeproj
DerivedData/
.DS_Store

# Vendored third-party JS/CSS, regenerated by scripts/vendor-highlightjs.sh
# from vendor/package.json + vendor/package-lock.json (see those files and
# THIRD_PARTY_LICENSES.md). Not committed, so a version bump is a one-line
# lockfile diff instead of a multi-thousand-line vendored-source diff.
Sources/Folium/Vendor/
vendor/node_modules/
